[project]
name = "llm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Koduru Nirmal Nethra", email = "knirmalnethra@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "ipython>=9.17.1",
    "openai>=3.7.0",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
llm = "llm:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
